{
  "name": "adrianomaringolo-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter portfolio dev",
    "dev:portfolio": "pnpm --filter portfolio dev",
    "dev:websites": "pnpm --filter websites dev",
    "build": "pnpm --filter portfolio build",
    "build:portfolio": "pnpm --filter portfolio build",
    "build:websites": "pnpm --filter websites build",
    "build:all": "pnpm -r build",
    "lint:all": "pnpm -r lint",
    "type-check:all": "pnpm -r type-check"
  }
}
